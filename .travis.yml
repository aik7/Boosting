sudo: required
language: cpp

os: linux
dist: bionic
compiler: gcc

addons:
  apt:
    packages:

      - gfortran-9
      
      # OpenMPI
      - openmpi-bin
      - libopenmpi-dev

# Handle git submodules
git:
    submodules: true

install:
  - sh scripts/build.sh

script:
  - ./boosting ../data/indian.dat
